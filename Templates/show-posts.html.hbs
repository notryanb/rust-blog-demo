<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Handlebars Demo</title>
  </head>
  <body>
    <h1>Here are the Posts</h1>
    <p>Rendering: {{data}}</p>

    <div>
      {{#each posts}}
        <h4>{{this.title}}</h4>
        <p>{{this.body}}</p>
      {{/each}}
    </div>

  </body>
</html>

