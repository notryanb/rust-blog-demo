<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Rust Blog</title>
  </head>
  <body>
  <a href="/">> Home</a>
    <h1>Here are the Posts</h1>
    <p>Rendering: {{data}}</p>

    <div>
      {{#each posts}}
        <h4>{{this.title}}</h4> <a href="/edit_post/{{this.id}}">Edit</a>
        <p>{{this.body}}</p>
      {{/each}}
    </div>

  </body>
</html>

